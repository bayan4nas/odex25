<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Skill Form View -->
    <record id="view_skill_form" model="ir.ui.view">
        <field name="name">skill.form</field>
        <field name="model">skill.skill</field>
        <field name="arch" type="xml">
            <form string="Skill">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="skill_type_id"/>
                            <field name="description"/>
                        </group>
                    </group>


                            <field widget="section_and_note_one2many" name="items_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="display_type" invisible="1"/>
                                    <field name="item_id" invisible="1"/>
                                    <field name="name" widget="section_and_note_text"/>
                                    <field name="level"/>
                                    <control>
                                        <create name="add_product_control" string="Add a Item "/>
                                        <create name="add_section_control" string="Add a section "
                                                context="{'default_display_type': 'line_section'}"/>
                                        <create name="add_note_control" string="Add a note"
                                                context="{'default_display_type': 'line_note'}"/>
                                    </control>
                                </tree>
                            </field>

                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Skill Tree View -->
    <record id="view_skill_tree" model="ir.ui.view">
        <field name="name">skill.tree</field>
        <field name="model">skill.skill</field>
        <field name="arch" type="xml">
            <tree string="Skills" expand="1">
                <field name="skill_type_id" />
                <field name="name"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <record id="view_skill_search" model="ir.ui.view">
        <field name="name">skill.skill.search</field>
        <field name="model">skill.skill</field>
        <field name="arch" type="xml">
            <search string="Skills">
                <!-- Search fields -->
                <field name="skill_type_id" string="Skill Type"/>
                <field name="name" string="Name" filter_domain="[('name', 'ilike', self)]"/>
                <separator/>

                <!-- Group By Section -->
                <group expand="0" string="Group By">
                    <filter name="group_by_skill_type"
                            string="Skill Type"
                            domain="[]"
                            context="{'group_by': 'skill_type_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="skill_search_view" model="ir.ui.view">
        <field name="name">kpi.skill.search</field>
        <field name="model">skill.skill</field>
        <field name="arch" type="xml">
            <search string="Skill">
                <field name="name"/>
            </search>
        </field>
    </record>

    <record id="skill_act_window" model="ir.actions.act_window">
        <field name="name">Skills</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">skill.skill</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_by_skill_type': 1}</field>
        <field name="search_view_id" ref="view_skill_search"/>
    </record>

    <record id="hr_job_from_extend" model="ir.ui.view">
        <field name="name">hr.job.extend</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_hr_job_form"/>
        <field name="arch" type="xml">

            <xpath expr="//notebook" position="inside">
                <page string="Skills">
                    <field name="item_job_ids" domain="[('display_type','=',False)]">
                        <tree string="item_tree_tree">
                            <field name="skill_id"/>
                            <field name="name"/>
                            <field name="level"/>
                        </tree>
                    </field>

                </page>
            </xpath>
        </field>
    </record>

    <record id="item_tree_view_tree" model="ir.ui.view">
        <field name="name">item.tree.view.tree</field>
        <field name="model">skill.item</field>
        <field name="arch" type="xml">
            <tree string="item_tree_tree">
                <field name="skill_id"/>
                <field name="name"/>
                <field name="level"/>
            </tree>
        </field>
    </record>

    <menuitem name="Skills" id="skill_menu" sequence="3" parent="exp_hr_appraisal.appraisal_configuration"
              groups="exp_hr_appraisal.group_appraisal_employee,exp_hr_appraisal.group_appraisal_manager"
              action="skill_act_window"/>

</odoo>
