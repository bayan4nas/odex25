<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="account_payment_form_inherit" model="ir.ui.view">
        <field name="name">account.payment.form.inherit</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="priority" eval="8"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_bank_id']" position="after">
                <field name="destination_journal_id" domain="[('type','in',('bank','cash'))]"
                       context="{'default_partner_id': partner_id}"
                       attrs="{'invisible': [('is_internal_transfer', '=', False)],'readonly': [('state', '!=', 'draft')], 'required': [('is_internal_transfer', '=', True),('state', '=', 'draft')]}"/>
                <field name="paired_internal_transfer_payment_id" invisible="1"/>

            </xpath>
            <xpath expr="//field[@name='destination_account_id']" position="attributes">
                <attribute name="required">0</attribute>
            </xpath>
            <xpath expr="//header" position="before">
                <div class="alert alert-info text-center" role="alert"
                     attrs="{'invisible': ['|','|',('paired_internal_transfer_payment_id','!=',False),('is_internal_transfer','=',False),('state','!=','draft')]}">
                    A second payment will be created automatically in the destination journal.
                </div>
            </xpath>
        </field>
    </record>
    <record id="view_account_invoice_form_inherit_payments_tab" model="ir.ui.view">
        <field name="name">account.move.form.inherit.payments.tab</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button  attrs="{'invisible':[('payment_state','not in',('paid','partial'))]}" name="action_view_payments"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-money">
                    <field name="payment_count" widget="statinfo" string="المدفوعات"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>

